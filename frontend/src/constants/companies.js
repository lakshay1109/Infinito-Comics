import logo from '../../assets/Images/SupportUs/credit.png';

const companies = {
    patron: Array(10).fill(logo),
    titanium: Array(5).fill(logo),
    platinum: Array(5).fill(logo),
    gold: Array(6).fill(logo),
};

export default companies;